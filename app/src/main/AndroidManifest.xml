<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appxemphim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ComboActivity"></activity>
        <activity android:name=".activity.ChonGheActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/background_gradient" />

        <activity android:name=".activity.SuatChieuActivity" />
        <activity android:name=".activity.ChiTietPhimActivity" />
        <activity android:name=".activity.ThongTinActivity" />
        <activity android:name=".activity.LienHeActivity" />
        <activity android:name=".activity.PhimTheoTheLoaiActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>